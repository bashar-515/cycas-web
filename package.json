{
  "name": "cycas-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "stage": "tsc && vite build --mode staging",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "oidc-client-ts": "^3.3.0"
  }
}
